"""Message bus building blocks (idempotency, retries, outbox/inbox dispatch).

Deliberately empty for now: broker wiring is out of scope for this milestone.
The RabbitMQ service is already provisioned in ``docker-compose.yaml`` so this
package can be filled in without touching the infrastructure setup.
"""
